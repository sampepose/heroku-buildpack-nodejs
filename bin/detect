#!/usr/bin/env bash
# bin/detect <build-dir>

read -p "Enter the subdirectory where the Node.js application is located (if none, leave blank):" SUB_DIR
BUILD_DIR="$1/$SUB_DIR"

read -n 1 HEY
echo "HEY:$HEY.";

a=0
read line
echo "Final line count is: $a";